apiVersion: v1
kind: Service
metadata:
  name: inference-api
  namespace: inference
spec:
  type: LoadBalancer
  selector:
    app: inference-api
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP

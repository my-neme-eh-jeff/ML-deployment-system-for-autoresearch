apiVersion: v1
kind: Service
metadata:
  name: churn-api
  namespace: churn-serving
spec:
  type: LoadBalancer
  selector:
    app: churn-api
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP

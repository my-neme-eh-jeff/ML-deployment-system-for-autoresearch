{
  "$schema": "https://webreel.dev/schema/v1.json",
  "outDir": "../../captures/raw",
  "viewport": "desktop",
  "defaultDelay": 400,
  "clickDwell": 140,
  "videos": {
    "kfp-dag": {
      "url": "${KFP_URL}/#/runs",
      "fps": 60,
      "quality": 85,
      "output": "02-kfp.mp4",
      "waitFor": { "text": "Run name" },
      "steps": [
        { "action": "pause", "ms": 1800, "label": "Kubeflow Pipelines • one run per iter" },
        { "action": "click", "selector": "table tbody tr:nth-of-type(1) a", "delay": 1800, "label": "open latest run", "description": "Click into the most recent KFP run — last autoresearch iter." },
        { "action": "wait", "text": "preprocess", "timeout": 10000 },
        { "action": "pause", "ms": 1600, "label": "preprocess → train → evaluate DAG", "description": "Settle on the DAG so the 3 green nodes are visible." },
        { "action": "click", "text": "preprocess", "delay": 1400, "label": "preprocess step", "description": "Click the preprocess node to open its side-panel." },
        { "action": "pause", "ms": 1400 },
        { "action": "click", "text": "Logs", "delay": 1500, "label": "preprocess logs", "description": "Show the actual training-side logs from this step." },
        { "action": "pause", "ms": 2000 },
        { "action": "click", "text": "train", "delay": 1500, "label": "train step", "description": "Switch focus to the train node." },
        { "action": "pause", "ms": 1400 },
        { "action": "click", "text": "Metrics", "delay": 1400, "label": "train metrics", "description": "Show the AUC / accuracy / F1 logged from this step to MLflow." },
        { "action": "pause", "ms": 1800 }
      ]
    }
  }
}

{
  "$schema": "https://webreel.dev/schema/v1.json",
  "outDir": "../../captures/raw",
  "viewport": "desktop",
  "defaultDelay": 400,
  "clickDwell": 140,
  "videos": {
    "mlflow-trajectory": {
      "url": "${MLFLOW_URL}/#/experiments/0",
      "fps": 60,
      "quality": 85,
      "output": "01-mlflow.mp4",
      "waitFor": { "text": "Experiments" },
      "steps": [
        { "action": "pause", "ms": 1800, "label": "MLflow • autoresearch runs", "description": "Settle on the runs list. Count varies per run — don't caption a specific number." },
        { "action": "scroll", "y": 200, "delay": 800, "description": "Show that more runs exist below the fold." },
        { "action": "scroll", "y": -200, "delay": 600, "description": "Scroll back to the top so the table header is visible." },
        { "action": "hover", "text": "auc_roc", "delay": 700, "label": "sort by AUC", "description": "Hover the AUC column header before clicking sort." },
        { "action": "click", "text": "auc_roc", "delay": 1200, "description": "Sort runs by AUC descending — best at top." },
        { "action": "pause", "ms": 1400, "label": "best run = current @champion candidate" },
        { "action": "click", "text": "Chart", "delay": 1500, "label": "Chart view" },
        { "action": "pause", "ms": 2200, "label": "AUC trajectory across iterations", "description": "Chart needs to be pre-configured in MLflow UI before recording — see RECORDING_SCRIPT.md preflight." },
        { "action": "click", "text": "Table", "delay": 1200, "description": "Back to the table." },
        { "action": "click", "selector": "table tbody tr:nth-of-type(1) a", "delay": 1500, "label": "open winning run", "description": "Click into the top run." },
        { "action": "wait", "text": "Parameters", "timeout": 8000 },
        { "action": "pause", "ms": 1400, "label": "Parameters + Metrics" },
        { "action": "scroll", "y": 400, "delay": 800, "description": "Scroll down to the Tags section." },
        { "action": "hover", "text": "kfp_run_id", "delay": 800, "label": "kfp_run_id tag", "description": "ID-MATCHING MOTIF — this tag's value is the same KFP run ID shown in the KFP scene and embedded in the PR body. Lineage is inspectable, not inferred." },
        { "action": "pause", "ms": 1800, "label": "linked to the KFP run that produced this model" },
        { "action": "scroll", "y": 200, "delay": 600 },
        { "action": "pause", "ms": 1200, "label": "Registered model: classifier @champion" }
      ]
    }
  }
}
